# Ollama (default: localhost; if WSL doesn't forward, use WSL IP from `wsl hostname -I`)
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=qwen3:8b

# Second Brain Sessions directory (auto-tracking AI coding work)
SECOND_BRAIN_SESSIONS=C:/Users/adamb/OneDrive/Dokumenty/SecondBrain/Sessions

# VAPID keys for Web Push (generate with `npm run vapid:gen`)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:adam.bytniewski@o2.pl

# Public version (exposed to client) of VAPID public key
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
